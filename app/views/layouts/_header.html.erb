<nav class="navbar navbar-expand-lg bg-dark">
  <div class="container-fluid">
    <div><%= link_to 'DaxTeams', root_path, class:"navbar-brand text-light" %></div>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" style="flex-grow:0;" id="navbarText">

      <ul class="navbar-nav me-auto mb-2 mb-lg-0  ">

        <% if user_signed_in? %>

          <% if current_user.admin? %>
            <li class="nav-item ">
              <div class="nav-link active" aria-current="page" ><%= link_to "Users", organization_users_path(1), class: "text-light"%></div>
            </li>

            <li class="nav-item">
                <div class="nav-link" ><%= link_to "Teams", organization_teams_path(1), class: "text-light" %></div>
            </li>

            <li class="nav-item">
                <div class="nav-link" ><%= link_to "Resources", organization_artifacts_path(1), class: "text-light" %></div>
             </li>
            <%end%>
             
            <% if current_user.team_leader? %>
               <li class="nav-item">
                <div class="nav-link" ><%= link_to "My Team", organization_teams_path(1), class: "text-light" %></div>
              </li>
            <%end%>

            <% if current_user.team_member? %>
              <li class="nav-item">
                <div class="nav-link" ><%= link_to "Resources", organization_artifacts_path(1), class: "text-light" %></div>
             </li>
             
            <% end %>
            
           <li class="nav-item  ">
              <div class="nav-link" ><%= link_to "Log out", destroy_user_session_path, method: "delete", class: "text-light" %></div>
            </li>
        <% else %>
           <li class="nav-item">
              <div class="nav-link active" aria-current="page" ><%= link_to "Sign In", new_user_session_path, class: "text-light" %></div>
            </li>
            <li class="nav-item">
              <div class="nav-link"><%= link_to "Register", new_user_registration_path, class: "text-light" %></div>
            </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>